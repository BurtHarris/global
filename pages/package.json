{
  "name": "pages-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "M365 Copilot Pages toolkit: core engine, CommonMark conversion, MCP server, CLI, and GUI",
  "workspaces": [
    "lib",
    "mcp",
    "cli",
    "gui"
  ],
  "scripts": {
    "build": "npm run --workspaces --if-present build",
    "test": "npm run --workspaces --if-present test",
    "typecheck": "npm run --workspaces --if-present typecheck"
  },
  "keywords": [
    "m365",
    "copilot-pages",
    "commonmark",
    "ast",
    "mcp"
  ],
  "author": "Burt Harris",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.5.0"
  }
}
